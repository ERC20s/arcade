// A game folder must hold one self-contained index.html and nothing else.
// This file exists so the checker's "no separate source files" rule has a
// known-bad case to fire on. It is never loaded by anything.
export const stray = true;
